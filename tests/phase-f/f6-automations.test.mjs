import test from'node:test';import assert from'node:assert/strict';import{runtime,lead}from'./helpers.mjs';
test('F6 derives overdue alert without external send',()=>{const alerts=runtime().buildInternalAlerts([lead()],[{lead_id:'lead-1',status:'agendado',scheduled_at:'2026-09-01',title:'Ligar'}],{stale_lead_days:7,follow_up_reminder_hours:24,alert_unassigned:true},new Date('2026-09-02'));assert.equal(alerts[0].type,'overdue');assert.equal('recipient'in alerts[0],false);});
test('F6 respects organization setting for unassigned leads',()=>assert.equal(runtime().buildInternalAlerts([lead({assigned_to:null})],[],{stale_lead_days:90,follow_up_reminder_hours:24,alert_unassigned:false},new Date('2026-09-02')).some(a=>a.type==='unassigned'),false));
test('F6 sorts high attention before lower attention',()=>{const alerts=runtime().buildInternalAlerts([lead({assigned_to:null})],[{lead_id:'lead-1',status:'agendado',scheduled_at:'2026-09-01',title:'Ligar'}],{stale_lead_days:7,follow_up_reminder_hours:24,alert_unassigned:true},new Date('2026-09-02'));assert.equal(alerts[0].level,'high');});
